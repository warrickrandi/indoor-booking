# Base URL of the API server, including the /api/v1 prefix (e.g. http://localhost:3001/api/v1)
NEXT_PUBLIC_API_URL=

# Base domain venue subdomains are provisioned under (e.g. yourplatform.com) —
# read by middleware.ts (Edge runtime) to detect tenant subdomains
NEXT_PUBLIC_PLATFORM_DOMAIN=
